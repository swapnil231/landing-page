import { TrimnewsoutletPipe } from './trimnewsoutlet.pipe';

describe('TrimnewsoutletPipe', () => {
  it('create an instance', () => {
    const pipe = new TrimnewsoutletPipe();
    expect(pipe).toBeTruthy();
  });
});
